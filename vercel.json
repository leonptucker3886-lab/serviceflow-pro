{
  "installCommand": "PRISMA_SKIP_POSTINSTALL_GENERATE=1 yarn install --frozen-lockfile --ignore-engines --network-timeout 600000",
  "buildCommand": "npx prisma generate && next build",
  "framework": "nextjs"
}
