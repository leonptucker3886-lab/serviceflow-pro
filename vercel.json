{
  "installCommand": "PRISMA_SKIP_POSTINSTALL_GENERATE=1 npm ci --legacy-peer-deps --no-audit --no-fund --prefer-online",
  "buildCommand": "npx prisma generate && next build",
  "framework": "nextjs"
}
